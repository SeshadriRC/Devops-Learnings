**Internet Gateway (IGW)** in AWS — **short and clear** 👇

> **An Internet Gateway is a VPC-level component that allows resources in a VPC to communicate with the internet.**

### Key points:

* Attached **to a VPC**
* Enables:

  * **Inbound internet traffic**
  * **Outbound internet traffic**
* Required for **public subnets**
* Works with **route tables** (`0.0.0.0/0 → IGW`)
* Supports **public IPv4 and IPv6**

### One-line interview answer:

> “An Internet Gateway connects a VPC to the internet and allows public subnets to send and receive internet traffic.”

That’s it 👍
